TODO for redo:
import statements for leaflet and cluster options
divide into separate files
consider all values of colorby column when parsing colorby list
more than one update function for renderSelected
assume variables are 'latitude'/'longitude'