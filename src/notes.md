TODO for redo:
import statements for leaflet and cluster options
divide into separate files
done? consider all values of colorby column when parsing colorby list
done? assume variables are 'latitude'/'longitude'